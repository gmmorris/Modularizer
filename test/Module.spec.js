var dom = require('./utils/dom.js'), chai = require('chai'), expect = chai.expect, assert = require('simple-assert');

describe('Modularizer Module management', function() {
  // Create a fake global `window` and `document` object and inject default script
  dom.init(true);

  describe('Modularizer load()', function() {
    it('', function() {
      assert(false);
    });
  });

  describe('Modularizer require()', function() {
    it('', function() {
      assert(false);
    });
  });
  describe('Modularizer knows()', function() {
    it('', function() {
      assert(false);
    });
  });
  describe('Modularizer fetchResource()', function() {

    it('', function() {
      assert(false);
    });
  });
  describe('Modularizer define()', function() {

    it('', function() {
      assert(false);
    });
  });

});

describe('Modularizer Module', function() {
  // Create a fake global `window` and `document` object and inject default script
  dom.init(true);

  describe('ready()', function() {
    it('should return false by default', function() {
      assert(false);
    });
    it('should receive a boolean value that changes its state and returns it', function() {
      assert(false);
    });
    it('should return the last boolean value sent to it as a parameter', function() {
      assert(false);
    });
  });
});
